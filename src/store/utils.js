export default ({
  migrations,
  names,
  languages,
  invites,
  notificationSettings,
  permissions,
  fungibleTokens,
}) => ({
  migrations,
  names,
  languages,
  invites,
  notificationSettings,
  permissions,
  fungibleTokens,
});
