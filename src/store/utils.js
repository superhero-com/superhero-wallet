export default ({
  migrations,
  names,
  languages,
  backedUpSeed,
  saveErrorLog,
  invites,
  notificationSettings,
  permissions,
  fungibleTokens,
  hiddenCards,
}) => ({
  migrations,
  names,
  languages,
  backedUpSeed,
  saveErrorLog,
  invites,
  notificationSettings,
  permissions,
  fungibleTokens,
  hiddenCards,
});
