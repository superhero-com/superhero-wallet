export default ({
  migrations,
  names,
  languages,
  backedUpSeed,
  mnemonic,
  saveErrorLog,
  invites,
  notificationSettings,
  permissions,
  fungibleTokens,
  accounts: { list, activeIdx, hdWallet: { nextAccountIdx } = {} } = {},
  hiddenCards,
}) => ({
  migrations,
  names,
  languages,
  backedUpSeed,
  mnemonic,
  saveErrorLog,
  invites,
  notificationSettings,
  permissions,
  fungibleTokens,
  accounts: { list, activeIdx, hdWallet: { nextAccountIdx } },
  hiddenCards,
});
