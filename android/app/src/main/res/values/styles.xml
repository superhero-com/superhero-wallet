<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Capacitor 8.4's now-active SystemBars plugin paints the DecorView with
             this windowBackground; on Android 15 edge-to-edge it shows through the
             transparent navigation bar. Keep it dark so the nav bar isn't white. -->
        <item name="android:windowBackground">#000000</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
      <item name="android:windowSplashScreenBackground" tools:targetApi="s">#000000</item>
      <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@drawable/splash_logo</item>
      <item name="android:navigationBarColor">#000000</item>
      <item name="android:windowLightNavigationBar">false</item>
      <item name="android:background">@drawable/launch_screen</item>
      <!-- Same as above: the activity keeps this theme after splash, and
           SystemBars paints the DecorView with windowBackground. -->
      <item name="android:windowBackground">#000000</item>
    </style>
</resources>